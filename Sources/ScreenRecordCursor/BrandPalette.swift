import SwiftUI

enum BrandPalette {
    /// The bright blue used for active product state. This is intentionally
    /// distinct from the navy brand color and remains legible in light and dark
    /// menu bars.
    static let brightBlue = Color(
        red: 10.0 / 255.0,
        green: 132.0 / 255.0,
        blue: 1
    )
}
